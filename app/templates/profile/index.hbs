{{#each articles key="id" as |article|}}
  <FeedItem @isLoggedIn={{session.isLoggedIn}} @article={{article}} @onFavoriteArticle={{action "favoriteArticle"}} />
{{else}}
  <div class="article-preview">
    No articles are here... yet.
  </div>
{{/each}}